@model QuestionViewModel

<fieldset>
    <legend>@Titles.ContentFlags</legend>

    @Titles.HasActiveFlags:
    @(Model.IsFlagged ? CommonTitles.Yes : CommonTitles.No)

    <br />
    <br />

    @using (Html.BeginCollection(() => Model.Flags))
    {
        foreach(var questionFlag in Model.Flags)
        {
            using (Html.BeginCollectionItem())
            {
                @Html.Partial(ViewNames._EditFlag, questionFlag);

                if (questionFlag != Model.Flags.Last())
                {
                    <br />
                }
            }
        }
    }
</fieldset>
